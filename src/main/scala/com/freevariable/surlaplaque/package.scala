package com.freevariable

import com.freevariable.surlaplaque.app

package object surlaplaque {
  val BucketApp = app.BucketApp
  val BucketClusterApp = app.BucketClusterApp
  val ReplHarness = app.ReplHarness
  val GPSClusterApp = app.GPSClusterApp
  val MMPClusterApp = app.MMPClusterApp
  val WaveletClusterApp = app.WaveletClusterApp
}